# Care Connect — install with: pip install -r requirements.txt

# Data extraction
requests>=2.31.0
beautifulsoup4>=4.12.0
lxml>=5.0.0
pyyaml>=6.0
click>=8.1.0
tenacity>=8.2.0
biopython>=1.83

# Chunking & embedding
tiktoken>=0.5.0
langchain-text-splitters>=0.2.0
sentence-transformers>=3.0.0
chromadb>=0.5.0

# API (FastAPI backend for UI)
fastapi>=0.115.0
uvicorn[standard]>=0.32.0

# OpenAI (for RAG when llm_provider is openai)
openai>=1.30.0

# Optional / dev
streamlit>=1.32.0
